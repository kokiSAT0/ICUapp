import React from 'react';
import { View, Text } from 'react-native';

// GCS のスコア計算コンポーネント
export default function GCSCalculator() {
  return (
    <View>
      {/* TODO: GCS 計算ロジックを追加 */}
      <Text>GCS 計算ツール (作成予定)</Text>
    </View>
  );
}
